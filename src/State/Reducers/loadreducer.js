
let  state =   { data:'sample'}

const AllCocktail=(state =  { data:'sample'} , action)=>{

      switch(action.type) {
         case "ALL_COCKTAIL" :
             return  state
         default :
        return  state  

            }
     
}
export  {AllCocktail};